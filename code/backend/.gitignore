# Node.js backend .gitignore

# Dependencies
# This directory contains all of your project's dependencies.
# It is generated by running `npm install` and should never be committed.
node_modules/
package-lock.json

# Environment Variables
# The .env file contains sensitive information like API keys and database credentials.
# It is crucial to keep this file out of version control.
# A .env.example file with the required variables should be committed instead.
.env
.env.local
data/
.env.development.local
.env.test.local
.env.production.local

# Logs
# Log files are generated during runtime and are specific to an environment.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build output
# If you use a transpiler like TypeScript, ignore the compiled output directory.
dist/
build/
.out/

# Test output
# Test runners often generate coverage reports.
coverage/
.nyc_output/

# Editor directories and files
# These are specific to your local development environment and editor.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# OS generated files
# Operating system files that are not part of the project.
.DS_Store
Thumbs.db